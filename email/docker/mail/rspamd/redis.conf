# Rspamd Redis backend — FlowOne Docker mail pod.
# Uses the pod-local redis-server (127.0.0.1:6379) supervised alongside Rspamd,
# matching the native box (a dedicated local redis for Bayes/ratelimit, separate
# from the app's redis container). No password (loopback-only).
servers = "127.0.0.1:6379";
