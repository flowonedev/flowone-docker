# Collab Server Configuration
# Rename this to .env and fill in your values
#
# IMPORTANT: These values MUST match your PHP backend config (backend/src/config.php)
# The collab server shares JWT and database credentials with the main application

# WebSocket server
COLLAB_WS_HOST=0.0.0.0
COLLAB_WS_PORT=1234

# Database connection (MariaDB) - MUST MATCH PHP CONFIG
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=devc_vps_dash
DB_USER=vpsadmin
DB_PASS=your_password_here

# JWT secret - MUST MATCH PHP CONFIG (jwt.secret)
# Copy from backend/src/config.php or use JWT_SECRET environment variable
JWT_SECRET=your_jwt_secret_here
JWT_ALGORITHM=HS256

# PHP Backend URL for permission verification
PHP_BACKEND_URL=https://email.devcon1.hu/api

# Redis for scaling (optional) - matches PHP config if set
# REDIS_HOST=127.0.0.1
# REDIS_PORT=6379
# REDIS_PASSWORD=

# Logging
LOG_LEVEL=info
