# /etc/tmpfiles.d/fleet-manager.conf
# Set permissions for Fleet Manager agent socket directory
# This runs on boot and ensures www-data can access the socket

d /run/fleet-manager 0750 root www-data -

